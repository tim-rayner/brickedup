# Mobile — agent guide

Expo app for Bricked Up. Product language: [../../CONTEXT.md](../../CONTEXT.md). Mobile-only terms: [CONTEXT.md](./CONTEXT.md). Monorepo guide: [../../AGENTS.md](../../AGENTS.md).

## Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v57.0.0/ before writing any code.

## Stack notes

- Entry: `expo-router` (`main`: `expo-router/entry`)
- App source: `src/`
- Prefer workspace packages (`packages/*`) over duplicating shared config or theme in the app

## Decisions

Record mobile (and most product) ADRs in [docs/adr/](./docs/adr/). Follow the three-criteria rule in that folder's README.
